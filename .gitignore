node_modules/
artifacts/
cache/
coverage/
dist/
.env

# deployments/ is deliberately NOT ignored: the records hold public addresses and are the
# canonical answer to "which contract is live on which chain".
